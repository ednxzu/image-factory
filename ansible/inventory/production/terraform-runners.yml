---
# production inventory for the ci-factory
plugin: generator
hosts:
  name: "terraform_{{ builder }}_{{ base }}_{{ version }}"
  parents:
    - name: "{{ builder }}_{{ base }}_{{ version }}"
      parents:
        - name: "{{ builder }}_{{ base }}"
          parents:
            - name: "{{ builder }}"
            - name: "{{ base }}"
    - name: "terraform"

layers:
  builder:
    - docker
  base:
    - node
  version:
    - 16
    - 18
    - 20
