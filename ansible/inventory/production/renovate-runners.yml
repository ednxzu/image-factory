---
# production inventory for the ci-factory
plugin: generator
hosts:
  name: "renovate_{{ builder }}_{{ base }}_{{ version }}"
  parents:
    - name: "{{ builder }}_{{ base }}_{{ version }}"
      parents:
        - name: "{{ builder }}_{{ base }}"
          parents:
            - name: "{{ builder }}"
            - name: "{{ base }}"
    - name: "renovate"

layers:
  builder:
    - docker
  base:
    - node
  version:
    - 18
    - 20
